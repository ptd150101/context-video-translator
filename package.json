{
  "name": "video-openai-realtime-translator",
  "version": "0.4.0",
  "private": true,
  "description": "Chrome MV3 extension: realtime bilingual subtitles for YouTube and Udemy using OpenAI-compatible translation, timedtext/VTT discovery, karaoke timing, furigana, and IndexedDB cache.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node -e \"import('node:fs/promises').then(fs=>fs.rm('dist',{recursive:true,force:true}))\""
  },
  "dependencies": {
    "kuromoji": "^0.1.2"
  }
}
